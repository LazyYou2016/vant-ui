const option = {
  textColor: '#2CC6ED',
  nameTextStyle: '#526C7E',
  xAxis: {
    axisLabel: {
      show: true,
      fontSize: 14,
      color: '#7A9CB3'
      // interval: 0
    }
  },
  yAxis: {
    splitLine: {
      show: true,
      lineStyle: {
        color: '#273642'
      }
    },
    splitArea: {
      show: false,
      areaStyle: {
        // color: ['#2E405B', '#1A2433'],
        // opacity: 0.8
      }
    },
    axisLabel: {
      show: true,
      fontSize: 16
    }
  }
}

export default option
